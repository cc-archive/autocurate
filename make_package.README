A little bash script that uses alien to generate little packages.

n.b., It excludes .svn from the generated packages.

REQUIREMENTS

* alien fakeroot

POTENTIAL PROBLEMS

* ownership in nested directories
* nested directories - does .svn get excluded?
